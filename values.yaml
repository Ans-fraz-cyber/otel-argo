# OpenTelemetry Demo Helm values for AWS public access

replicaCount: 1

frontend-proxy:
  service:
    type: LoadBalancer
    port: 80

frontend:
  service:
    type: LoadBalancer
    port: 80

ad:
  service:
    type: ClusterIP

cart:
  service:
    type: ClusterIP

checkout:
  service:
    type: ClusterIP

currency:
  service:
    type: ClusterIP

email:
  service:
    type: ClusterIP

flagd:
  service:
    type: ClusterIP

grafana:
  service:
    type: LoadBalancer
    port: 80

image-provider:
  service:
    type: ClusterIP

jaeger-agent:
  service:
    type: ClusterIP

jaeger-collector:
  service:
    type: ClusterIP

jaeger-query:
  service:
    type: LoadBalancer
    port: 16686

kafka:
  service:
    type: ClusterIP

load-generator:
  service:
    type: ClusterIP

opensearch:
  service:
    type: ClusterIP

opensearch-headless:
  service:
    type: ClusterIP

otel-collector:
  service:
    type: ClusterIP

payment:
  service:
    type: ClusterIP

postgresql:
  service:
    type: ClusterIP

product-catalog:
  service:
    type: ClusterIP

prometheus:
  service:
    type: LoadBalancer
    port: 9090

quote:
  service:
    type: ClusterIP

recommendation:
  service:
    type: ClusterIP

shipping:
  service:
    type: ClusterIP

valkey-cart:
  service:
    type: ClusterIP
